1) ^ are bitwise exclusive OR operations

2) A FileInputStream obtains input bytes from a file in a file system.
FileInputStream is meant for reading streams of raw bytes such as image data.

3) FileInputStream inputStream = new FileInputStream("hello.txt");

4) read() = reads the next byte. Returns the next byte or -1 if the end of the
file is reached.

5) This class overrides all methods of OutputStream with versions that pass all requests to the underlying output stream.

6) FileOutputStream outputStream = new FileOutputStream("hello.txt");

7) write(int)

8) close()

9) 

public static void main(String[] args) {
	String[] asd;
	String a = args[0];
	String b = args[1];
}

public writeMessage(String word, int num){
          num * (System.out.println(word));
          }
